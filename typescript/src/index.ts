const inner_sieve = (p: number, sieve: Array<number>): Array<Number> => {
  return sieve.filter((n: number) => n % p !== 0 || n == p);
}

const siever = (n: number) => {
  let p = 2;
}

const main = () => {
  console.log('hello typescript!')
}


main()
